# === БАЗА ДАННЫХ (переменные окружения для Railway) ===
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://postgres:Qwqwerty121!!12@db.ahmjnonuivkaschorydy.supabase.co:5432/postgres?sslmode=require}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:Qwqwerty121!!12}

# === HIBERNATE ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false

# === ИНИЦИАЛИЗАЦИЯ ДАННЫХ ===
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# === ФАЙЛЫ ===
file.upload-dir=./uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# === ПОРТ ===
server.port=${PORT:8080}